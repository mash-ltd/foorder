- for item in @cart.items
  %tr{class: "#{item == @current_item ? "current_item" : ""}"}
    %td
      = item.quantity
    %td
      = item.name
    %td
      .pull-right
        = number_to_currency(item.price, unit: "EGP", format: "%n %u")